



#include "ft_printf.h"
#include <limits.h>

int main()
{
	ft_printf(NULL);


    return 0;
}

	// int check = ((c == 'c') * 1 + (c == 's') * 2);
	// (c == 'c') && (print_char(va_arg(ap, int), &count), 0);
